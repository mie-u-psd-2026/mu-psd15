# テスト仕様書: クレトレ

作成日: 2026年9月3日 / 作成者: 中川 颯都(チーム15)

## 1. 何をテストするか

| 対象 | 中身 |
|------|------|
| `app.py` | サーバー側。3つのAPIの窓口 |
| `static/index.html` | 画面側。Vue.jsで作った3つの場面 |
| `tests/test_app.py` | 自動テスト。AIを差し替えて判定ロジックとAPIを確かめる |

## 2. テストする環境

| 項目 | 内容 |
|------|------|
| OS | macOS 15(Apple M5 / メモリ16GB) |
| Python | 3.13 |
| AI | Ollama + gemma2:9b(パソコンの中で動く。ネット接続不要) |
| アクセス先 | http://localhost:5001 |
| ブラウザ | Google Chrome / Safari の最新版 |

**注意**: macOSではポート5000をAirPlayが使っているため、5001を使います。

## 3. テストのやり方

### 3.1 準備

```bash
cd mu-psd15
pip install -r requirements.txt
ollama pull gemma2:9b
python app.py
```

ブラウザで http://localhost:5001 を開きます。

### 3.2 APIを直接叩く場合

画面を通さずサーバーだけを試すときは、ターミナルから次のように送ります。

```bash
curl -X POST http://localhost:5001/api/turns \
  -H "Content-Type: application/json" \
  -d '{"scenario":"product_defect","anger":80,"turn":0,"messages":[],"reply":"申し訳ございません。"}'
```

## 4. 正常系テスト(うまくいく場合)

期待どおりに動くことを確かめます。

| No | テスト内容 | 事前条件 | 手順 | 期待する結果 |
|----|-----------|---------|------|-------------|
| N-1 | サーバーが起動する | Ollama が起動し gemma2:9b が取得済み | `python app.py` を実行 | エラーなく起動し、5001番で待ち受ける |
| N-2 | 画面が表示される | サーバー起動済み | ブラウザで `/` を開く | 「クレトレ」の準備画面が出る |
| N-3 | 練習を開始できる | 準備画面を表示中 | 場面を選び「練習を始める」を押す | 会話画面に切り替わり、お客さんの第一声が出る |
| N-4 | 難易度で初期値が変わる | 準備画面を表示中 | やさしめ / ふつう / きびしめ を選んで開始 | 怒りが 60 / 80 / 95 で始まる |
| N-5 | 良い応対で怒りが下がる | 会話中(怒り80) | 謝罪と解決策を含む返事を送る | 怒りの数字が下がる |
| N-6 | 悪い応対で怒りが上がる | 会話中(怒り80) | 「お客様の使い方が悪いのでは」と送る | 怒りの数字が上がる |
| N-7 | 会話が積み上がる | 会話中 | 何回かやり取りする | 過去の発言が消えず、下に追加されていく |
| N-8 | 怒りメーターの色が変わる | 会話中 | 怒りを 20/50/80 の状態にする | 緑 / 黄 / 赤 に変わる |
| N-9 | 成功で終われる | 会話中(怒り30以下) | 怒りを15以下まで下げる | 「お客さんを落ち着かせました」と出る |
| N-10 | 講評が出る | 会話が終了した直後 | 会話を終える | 3項目の星と、文章の講評が出る |
| N-11 | もう一度できる | 結果画面を表示中 | 「もう一度」を押す | 準備画面に戻り、前の会話が消えている |
| N-12 | 3つの場面すべてで動く | 準備画面を表示中 | 場面を変えて開始 | どの場面でも第一声が変わって始まる |
| N-13 | 振り返りが出る | 1回以上返答して終了 | 会話を終える | 怒りの折れ線と、返答ごとの増減が出る |
| N-14 | 効いた一言が分かる | 良い返答と悪い返答を各1回以上送って終了 | 良い返答と悪い返答を混ぜて終える | 「いちばん効いた一言」「いちばん怒らせた一言」が正しく付く |
| N-15 | 返答なしで中断 | 開始直後(返答0回) | 開始してすぐ中断する | 振り返りは表示されず、講評だけが出る |

## 5. 異常系テスト(うまくいかない場合)

失敗したときに、アプリが壊れず、理由が分かることを確かめます。

| No | テスト内容 | 事前条件 | 手順 | 期待する結果 |
|----|-----------|---------|------|-------------|
| E-1 | 返事が空 | 会話中 | 何も入力せず送信 | 送信ボタンが押せない |
| E-2 | 空白だけの返事 | サーバー起動済み(API直接) | スペースだけ入れてAPIに送る | HTTP 400 と「応対内容を入力してください。」 |
| E-3 | 場面の指定が不正 | サーバー起動済み(API直接) | `scenario` に存在しない値を送る | HTTP 400 と「場面の指定が正しくありません。」 |
| E-4 | 項目が足りない | サーバー起動済み(API直接) | 空の `{}` を送る | HTTP 400 が返る |
| E-5 | 履歴なしで講評 | サーバー起動済み(API直接) | `messages` を空にして送る | HTTP 400 と「会話の履歴が空です。」 |
| E-6 | サーバーが止まっている | 会話中に入力済み、サーバー停止 | サーバーを止めてから送信 | 日本語のエラーが出て、**入力した文章が消えない** |
| E-7 | AIが止まっている | サーバー起動済み、Ollama 停止 | Ollamaを止めてから送信 | HTTP 500 と「AIとの通信中にエラーが発生しました。」 |
| E-8 | AIの返事の形が崩れる | 会話中(自動テストでAIの返事を差し替え) | `【怒り】`のない返事が来た場合 | アプリが止まらず、直前の怒り値のまま会話が続く |

## 6. 境界値テスト(ぎりぎりの値)

区切りの数字ちょうどで、判定が正しく切り替わるかを確かめます。

| No | 条件 | 期待する結果 |
|----|------|-------------|
| B-1 | 怒りが **15** ちょうど | 成功(resolved)になる |
| B-2 | 怒りが **16** | まだ続く(continue) |
| B-3 | 怒りが **100** に達した | 失敗(escalated)になる |
| B-4 | 怒りが 100 を超える値をAIが返した | 100 に丸められる |
| B-5 | 怒りが 0 未満の値をAIが返した | 0 に丸められる |
| B-6 | やり取りが **12回目** | 打ち切り(escalated)になる |
| B-7 | やり取りが **11回目** | まだ続く(continue) |
| B-8 | 怒り **30 / 31** | 緑 / 黄 に切り替わる |
| B-9 | 怒り **70 / 71** | 黄 / 赤 に切り替わる |

## 7. 画面の状態テスト(4大状態)

設計書で決めた4つの状態が、それぞれ正しく見えるかを確かめます。

| No | 状態 | 手順 | 期待する結果 |
|----|------|------|-------------|
| S-1 | 通常 | 会話画面を開く | 入力できる。未入力だと送信ボタンが押せない |
| S-2 | 処理中 | 送信直後 | ぐるぐるが回り、「お客さんが考えています…」が出る。入力欄が使えなくなる |
| S-3 | 成功 | 返事が返ってきたとき | 会話に追加され、メーターがなめらかに動く |
| S-4 | 異常 | 通信に失敗したとき | 赤い枠でエラーが出て、「もう一度送る」ボタンが出る |

## 8. 非機能要件のテスト

要件定義で約束した「速さ」や「安全さ」を確かめます。

| No | 項目 | 確かめ方 | 合格の基準 |
|----|------|---------|-----------|
| P-1 | 応答の速さ | 送信から返事まで測る | **5秒以内** |
| P-2 | ネットなしで動く | Wi-Fiを切って操作する | 問題なく動く |
| P-3 | 日本語で返る | 何度か送信して返事を見る | 英語などが混ざらない |
| P-4 | 入力を保存しない | サーバーのログとファイルを見る | 入力した文章が残っていない |

## 9. 自動テスト(pytest)

上の表のうち、AIの返事に左右されないものは `tests/test_app.py` に自動テストとして書いてあります。

自動テストでは、AIへの問い合わせを**モック**に差し替えます。モックとは「本物のふりをするニセ物」のことです。ここでは、AIに聞きに行く部分だけを「いつも決まった文章を返すだけの部品」に取りかえます。本物のAIを呼ばないので、次の3つが手に入ります。

| モックの利点 | この作品での意味 |
|---|---|
| 速い | AIの返事を待たないので、全部で1秒かからない |
| AIが要らない | Ollama を起動していなくてもテストできる |
| 狙った返事を作れる | 「AIが `-5` と返してきたら」のような、手では起こせない場面を作れる |

実行は次の1行です。

```bash
pytest
```

| 自動化したもの | 対応する番号 | ねらい |
|---|---|---|
| 終了判定の境界 | B-1, B-2, B-3, B-6, B-7 | 15/16、12回目/11回目のちょうど境目を機械的に確かめる |
| AIの返事の丸め | B-4, B-5 | 150 や -5 のような値は手動では狙って起こせない |
| 1回の変化幅の制限 | (仕様 E章) | 12以内に抑えられること |
| 形の崩れた返事 | E-8、不具合1 | 【怒り】の行が本文に混ざらない、直前の値を保つ |
| APIの入力チェック | E-2, E-3, E-4, E-5 | 400 と日本語メッセージ |
| AIが止まっている | E-7 | 500 と Problem Details 形式 |
| 正常系の入出力 | N-3, N-4、講評 | 難易度ごとの初期値、status の遷移、点数の読み取り |

しきい値は `app.py` の定数(`RESOLVED_ANGER` など)から読むので、値を変えてもテストを書き直す必要はありません。

### 自動テストで見つかった不具合

B-5(怒りが 0 未満)は手動では再現できず、自動テストで初めて確かめました。結果、`【怒り】-5` のようにマイナスが付くと数字として読み取れず、**0 に丸めるのではなく直前の値のまま**になっていました。

原因は**正規表現**にありました。正規表現とは「こういう形の文字を探して」と指定するための書き方です。このアプリでは、AIの返事の中から `【怒り】` の後ろにある数字を取り出すために使っています。その指定が「数字だけ」になっていたため、`-5` の先頭にあるマイナス記号を数字の一部と見なせず、見つけられませんでした。マイナスが付いていてもよい形に直しています(不具合4)。

## 10. テストの結果

実施日: 2026年9月3日 / 実施者: 中川 颯都

| 分類 | 件数 | 合格 | 不合格 | 備考 |
|------|------|------|--------|------|
| 正常系 | 15 | 15 | 0 | N-13〜N-15 は振り返り機能ぶん |
| 異常系 | 8 | 8 | 0 | |
| 境界値 | 9 | 9 | 0 | |
| 画面の状態 | 4 | 4 | 0 | ブラウザで目視確認 |
| 非機能 | 4 | 4 | 0 | P-1 は修正後に再測定して合格 |
| 自動テスト(pytest) | 26 | 26 | 0 | 9/4 追加。B-5 の不具合を修正後に全件合格 |

### 主な計測値

| 項目 | 結果 |
|------|------|
| 応答速度(5回計測) | 最遅 3.3秒 / 平均 1.8秒(基準の5秒以内) |
| 外国語の混入(10回計測) | 0回 |
| 良い応対での怒りの変化 | 80 → 60 |
| 不誠実な応対での怒りの変化 | 80 → 95 |

## 11. 見つかった不具合と対応

| No | 不具合の内容 | 原因 | 対応 |
|----|-------------|------|------|
| 1 | AIの返事に `【怒り】60` が混ざって画面に出る | AIが `【セリフ】` を省くことがあり、そのとき返事の全文を本文として扱っていた | `【怒り】` の行を取り除いた残りを本文とする処理に変更 |
| 2 | 通信失敗時に英語で "Failed to fetch" と表示される | サーバーに届かない場合はブラウザ既定の例外がそのまま出ていた | 日本語のメッセージに置き換えた |
| 3 | **応答に最大95秒かかることがある** | Ollamaが5分間使われないとモデルをメモリから解放し、次の1回目に読み込みが発生する | サーバー起動時にモデルを読み込み、`keep_alive` で常駐させる処理を追加 |
| 4 | 怒りが 0 未満の値で返っても 0 に丸められない | 数字を探す指定(正規表現)がマイナス記号を数字の一部と見なしていなかった | マイナスが付いてもよい形に変更。自動テスト B-5 で見つかった |

### 不具合3について

要件で約束した「5秒以内」を大きく超えており、発表のデモでも起こりうる重大な問題でした。

AIを常駐させる指定(`keep_alive`)は、はじめ OpenAI 互換の窓口ごしに渡していました。しかし `ollama ps` で確かめると、設定が効いていませんでした。この窓口では `keep_alive` が読み飛ばされるためです。そこで Ollama 本来の窓口へ直接渡す形に変えました。

修正の前後で、次のように速くなりました。

| | 修正前 | 修正後 |
|---|---|---|
| 解放直後の1回目 | 20〜95秒 | 3.3秒 |
| 2回目以降 | 2〜3秒 | 1.2〜1.6秒 |

## 12. 今後の課題

- 実機の別ブラウザ(Safari)での確認は未実施
- 長い文章(1000字以上)を送ったときの挙動は未確認
- 複数人が同時に使う場合の負荷は、今回の利用想定に含まれないため未実施
