# テスト仕様書: クレトレ

作成日: 2026年9月3日 / 作成者: 中川 隼人(チーム15)

## 1. 何をテストするか

| 対象 | 中身 |
|------|------|
| `app.py` | サーバー側。3つのAPIの窓口 |
| `static/index.html` | 画面側。Vue.jsで作った3つの場面 |

## 2. テストする環境

| 項目 | 内容 |
|------|------|
| OS | macOS 15(Apple M5 / メモリ16GB) |
| Python | 3.13 |
| AI | Ollama + gemma2:9b(パソコンの中で動く。ネット接続不要) |
| アクセス先 | http://localhost:5001 |
| ブラウザ | Google Chrome / Safari の最新版 |

**注意**: macOSではポート5000をAirPlayが使っているため、5001を使います。

## 3. テストのやり方

### 3.1 準備

```bash
cd mu-psd15
pip install -r requirements.txt
ollama pull gemma2:9b
python app.py
```

ブラウザで http://localhost:5001 を開きます。

### 3.2 APIを直接叩く場合

画面を通さずサーバーだけを試すときは、ターミナルから次のように送ります。

```bash
curl -X POST http://localhost:5001/api/turns \
  -H "Content-Type: application/json" \
  -d '{"scenario":"product_defect","anger":80,"turn":0,"messages":[],"reply":"申し訳ございません。"}'
```

## 4. 正常系テスト(うまくいく場合)

期待どおりに動くことを確かめます。

| No | テスト内容 | 手順 | 期待する結果 |
|----|-----------|------|-------------|
| N-1 | サーバーが起動する | `python app.py` を実行 | エラーなく起動し、5001番で待ち受ける |
| N-2 | 画面が表示される | ブラウザで `/` を開く | 「クレトレ」の準備画面が出る |
| N-3 | 練習を開始できる | 場面を選び「練習を始める」を押す | 会話画面に切り替わり、お客さんの第一声が出る |
| N-4 | 難易度で初期値が変わる | やさしめ / ふつう / きびしめ を選んで開始 | 怒りが 60 / 80 / 95 で始まる |
| N-5 | 良い応対で怒りが下がる | 謝罪と解決策を含む返事を送る | 怒りの数字が下がる |
| N-6 | 悪い応対で怒りが上がる | 「お客様の使い方が悪いのでは」と送る | 怒りの数字が上がる |
| N-7 | 会話が積み上がる | 何回かやり取りする | 過去の発言が消えず、下に追加されていく |
| N-8 | 怒りメーターの色が変わる | 怒りを 20/50/80 の状態にする | 緑 / 黄 / 赤 に変わる |
| N-9 | 成功で終われる | 怒りを20以下まで下げる | 「お客さんを落ち着かせました」と出る |
| N-10 | 講評が出る | 会話を終える | 3項目の星と、文章の講評が出る |
| N-11 | もう一度できる | 「もう一度」を押す | 準備画面に戻り、前の会話が消えている |
| N-12 | 3つの場面すべてで動く | 場面を変えて開始 | どの場面でも第一声が変わって始まる |

## 5. 異常系テスト(うまくいかない場合)

失敗したときに、アプリが壊れず、理由が分かることを確かめます。

| No | テスト内容 | 手順 | 期待する結果 |
|----|-----------|------|-------------|
| E-1 | 返事が空 | 何も入力せず送信 | 送信ボタンが押せない |
| E-2 | 空白だけの返事 | スペースだけ入れてAPIに送る | HTTP 400 と「応対内容を入力してください。」 |
| E-3 | 場面の指定が不正 | `scenario` に存在しない値を送る | HTTP 400 と「場面の指定が正しくありません。」 |
| E-4 | 項目が足りない | 空の `{}` を送る | HTTP 400 が返る |
| E-5 | 履歴なしで講評 | `messages` を空にして送る | HTTP 400 と「会話の履歴が空です。」 |
| E-6 | サーバーが止まっている | サーバーを止めてから送信 | 日本語のエラーが出て、**入力した文章が消えない** |
| E-7 | AIが止まっている | Ollamaを止めてから送信 | HTTP 500 と「AIとの通信中にエラーが発生しました。」 |
| E-8 | AIの返事の形が崩れる | `【怒り】`のない返事が来た場合 | アプリが止まらず、直前の怒り値のまま会話が続く |

## 6. 境界値テスト(ぎりぎりの値)

区切りの数字ちょうどで、判定が正しく切り替わるかを確かめます。

| No | 条件 | 期待する結果 |
|----|------|-------------|
| B-1 | 怒りが **20** ちょうど | 成功(resolved)になる |
| B-2 | 怒りが **21** | まだ続く(continue) |
| B-3 | 怒りが **100** に達した | 失敗(escalated)になる |
| B-4 | 怒りが 100 を超える値をAIが返した | 100 に丸められる |
| B-5 | 怒りが 0 未満の値をAIが返した | 0 に丸められる |
| B-6 | やり取りが **10回目** | 打ち切り(escalated)になる |
| B-7 | やり取りが **9回目** | まだ続く(continue) |
| B-8 | 怒り **30 / 31** | 緑 / 黄 に切り替わる |
| B-9 | 怒り **70 / 71** | 黄 / 赤 に切り替わる |

## 7. 画面の状態テスト(4大状態)

設計書で決めた4つの状態が、それぞれ正しく見えるかを確かめます。

| No | 状態 | 手順 | 期待する結果 |
|----|------|------|-------------|
| S-1 | 通常 | 会話画面を開く | 入力できる。未入力だと送信ボタンが押せない |
| S-2 | 処理中 | 送信直後 | ぐるぐるが回り、「お客さんが考えています…」が出る。入力欄が使えなくなる |
| S-3 | 成功 | 返事が返ってきたとき | 会話に追加され、メーターがなめらかに動く |
| S-4 | 異常 | 通信に失敗したとき | 赤い枠でエラーが出て、「もう一度送る」ボタンが出る |

## 8. 非機能要件のテスト

要件定義で約束した「速さ」や「安全さ」を確かめます。

| No | 項目 | 確かめ方 | 合格の基準 |
|----|------|---------|-----------|
| P-1 | 応答の速さ | 送信から返事まで測る | **5秒以内** |
| P-2 | ネットなしで動く | Wi-Fiを切って操作する | 問題なく動く |
| P-3 | 日本語で返る | 何度か送信して返事を見る | 英語などが混ざらない |
| P-4 | 入力を保存しない | サーバーのログとファイルを見る | 入力した文章が残っていない |

## 9. テストの結果

実施日: 2026年9月3日 / 実施者: 中川 隼人

| 分類 | 件数 | 合格 | 不合格 | 備考 |
|------|------|------|--------|------|
| 正常系 | 12 | 12 | 0 | |
| 異常系 | 8 | 8 | 0 | |
| 境界値 | 9 | 9 | 0 | |
| 画面の状態 | 4 | 4 | 0 | ブラウザで目視確認 |
| 非機能 | 4 | 4 | 0 | P-1 は修正後に再測定して合格 |

### 主な計測値

| 項目 | 結果 |
|------|------|
| 応答速度(5回計測) | 最遅 3.3秒 / 平均 1.8秒(基準の5秒以内) |
| 外国語の混入(10回計測) | 0回 |
| 良い応対での怒りの変化 | 80 → 60 |
| 不誠実な応対での怒りの変化 | 80 → 95 |

## 10. 見つかった不具合と対応

| No | 不具合の内容 | 原因 | 対応 |
|----|-------------|------|------|
| 1 | AIの返事に `【怒り】60` が混ざって画面に出る | AIが `【セリフ】` を省くことがあり、そのとき返事の全文を本文として扱っていた | `【怒り】` の行を取り除いた残りを本文とする処理に変更 |
| 2 | 通信失敗時に英語で "Failed to fetch" と表示される | サーバーに届かない場合はブラウザ既定の例外がそのまま出ていた | 日本語のメッセージに置き換えた |
| 3 | **応答に最大95秒かかることがある** | Ollamaが5分間使われないとモデルをメモリから解放し、次の1回目に読み込みが発生する | サーバー起動時にモデルを読み込み、`keep_alive` で常駐させる処理を追加 |

### 不具合3について

要件で約束した「5秒以内」を大きく超えており、発表デモでも起こりうる重大な問題だった。

当初は `keep_alive` をOpenAI互換API経由で指定したが、`ollama ps` で確認したところ設定が反映されていなかった。OpenAI互換APIでは `keep_alive` が無視されるため、Ollama純正のAPIへ直接指定する形に変更した。

修正の前後で次のように改善した。

| | 修正前 | 修正後 |
|---|---|---|
| 解放直後の1回目 | 20〜95秒 | 3.3秒 |
| 2回目以降 | 2〜3秒 | 1.2〜1.6秒 |

## 11. 今後の課題

- 実機の別ブラウザ(Safari)での確認は未実施
- 長い文章(1000字以上)を送ったときの挙動は未確認
- 複数人が同時に使う場合の負荷は、今回の利用想定に含まれないため未実施
